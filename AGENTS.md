# AGENTS.md

## 目的

このリポジトリでは、React ベースのウェブアプリとして、企画提案やモック提示に使えるプロトタイプを構築する。

初期版で実現する体験は以下。

- ユーザーが自分の API キーを入力する
- テキストファイルと PDF をアップロードする
- 検索フレーズを入力する
- ベクトル検索と一般的な RAG 的な関連度計算を使って、関連度の高い順に並び替える
- 特に PDF については、どのページが最も関連しているかを直感的に示す

この `AGENTS.md` は、以後の実装判断の基準とする。

## 固定方針

- フロントエンドは React
- 初期版は API キー入力のみで開始する
- 初期対応ファイルはテキストと PDF のみ
- Google Drive や外部取り込みは一切考慮しない
- UI は白基調、分かりやすさ優先、レスポンシブ前提
- 企画書代わりに見せられる、シンプルで理解しやすい体験を優先する

## 参照ドキュメント

2026-03-11 時点で、以下のローカル資料を正式な前提として扱う。

- [text-embed2-official-document.txt](/Users/igaki/Documents/GitHub/vector-file-search/text-embed2-official-document.txt)

この資料は、2026 年 3 月 10 日公開の Google Cloud 公式ドキュメントのコピーであり、`gemini-embedding-2-preview` の仕様を含む。

## モデル前提

初期版では、埋め込みモデルとして以下を採用する。

- モデル ID: `gemini-embedding-2-preview`

上記ドキュメントから採用する重要仕様:

- マルチモーダル入力に対応
- 入力としてテキスト、画像、音声、動画、PDF を受け付ける
- ベクトル出力は最大 3,072 次元
- `output_dimensionality` により出力次元数の調整が可能
- PDF を直接入力できる
- ドキュメント OCR を読む
- ドキュメントは 1 リクエストあたり 1 ファイル、最大 6 ページ
- 最大入力長は 8,192 トークン
- 公開プレビューであり、安定性よりも試作向けの位置づけ

## 初期版のスコープ

### 対応するもの

- テキスト入力の検索
- テキストファイルの埋め込み化
- PDF のページ単位ベクトル化
- 関連度スコアの算出
- スコア順の並び替え
- PDF の最関連ページ表示
- レスポンシブなデモ UI

### 対応しないもの

- Google Drive 連携
- Google Docs / Sheets / Slides 取り込み
- 画像検索 UI
- 音声 / 動画
- ベクトル DB
- ログイン
- 永続保存
- 大規模ファイル処理

## 検索の考え方

このアプリは、一般的な RAG に近い検索体験を、分かりやすい UI で見せることを目的にする。

### 採用する検索方式

- ユーザーの検索フレーズをベクトル化する
- 各ファイル、または PDF の各ページをベクトル化する
- ベクトル内積または cosine similarity を使って意味的関連度を計算する
- 必要に応じて、完全一致や部分一致などの lexical score を補助的に使う
- 最終スコアを数値化し、高い順に並べる

### 初期版の推奨スコアリング

初期版では、以下のハイブリッドスコアを基本とする。

- semantic score: ベクトル類似度
- lexical score: 完全一致、部分一致、出現回数などの軽量テキスト一致
- final score: `0.8 * semantic + 0.2 * lexical`

理由:

- いわゆる一般的な RAG 検索として説明しやすい
- 意味検索を主体にしつつ、明確な語句一致も拾える
- デモで結果が分かりやすい

ただし、初期実装では複雑な BM25 までは必須にしない。まずは軽量な lexical score で十分。

## ファイルごとのベクトル化方針

### テキストファイル

対象:

- `.txt`

方針:

- ファイル全体を文字列として読む
- 長文の場合は適切な長さで分割する
- 分割単位ごとにベクトル化する
- ファイル全体の代表スコアは、最上位セグメントのスコアを使う

### PDF

方針:

- PDF はページ単位で分割する
- 各ページを 1 単位としてベクトル化する
- ページごとの関連度を計算する
- PDF 全体の代表スコアは、最も高いページのスコアを採用する
- 検索結果では「どの PDF の何ページが最も関連したか」を明示する

補足:

- 初期版では「PDF は 1 枚ずつ内容を分割してベクトル化する」という要件を最優先する
- テキスト抽出結果を使う設計ではなく、ページ単位の処理を設計上の基本にする


## 埋め込み設計

### 次元数

初期版のデフォルトは以下。

- `output_dimensionality = 3,072`


必要に応じて将来以下を切り替え可能にする。

- 768
- 1536
- 3072

### 分割戦略

初期版の単位は次のとおり。

- テキスト: セグメント単位
- PDF: ページ単位

長いテキストファイルでは、単一ベクトルではなく複数セグメントに分割する。

推奨:

- 800 から 1,200 文字程度で分割
- 100 から 200 文字程度のオーバーラップを許容

## 類似度計算

### 基本

- ベクトル比較は内積または cosine similarity を使う
- 実装はまず cosine similarity を標準とする
- 正規化済みベクトルを使う場合は内積でもよい

### ファイルの最終順位

テキストファイル:

- 各セグメントのスコアを算出
- 最高スコアのセグメントを代表としてファイル順位を決める

PDF:

- 各ページのスコアを算出
- 最高スコアのページを代表として PDF 順位を決める

表示時には以下を必ず出す。

- ファイル名
- 最終スコア
- 一致したセグメントまたはページ
- 抜粋テキスト

## UI 方針

### デザイン原則

- 白基調
- 余白をしっかり取り、情報を整理する
- 一目で手順が分かる
- 技術者でなくても理解できる
- モバイルでも破綻しない

### 画面構成

#### 1. ヘッダー

- アプリ名
- 簡単な説明
- 「API キーは保存しない」明記

#### 2. ステップ UI

以下を縦並びまたは広い画面では 2 カラムで表示する。

1. API キー入力
2. ファイルアップロード
3. 検索フレーズ入力
4. 結果表示

#### 3. 結果表示

結果表示では以下を優先する。

- 関連度順ランキング
- PDF の最関連ページ番号
- テキスト抜粋
- 類似度スコアの視覚表示

#### 4. 見せ場

提案用の価値が伝わるよう、以下の見せ方を入れる。

- 「最も関連度が高いファイル」
- 「最も関連度が高い PDF ページ」
- 検索語に対して、どの部分が近いかの説明
- スコアバーやバッジで直感的に比較可能にする

## 体験フロー

1. ユーザーが API キーを入力する
2. 接続確認を行う
3. テキストまたは PDF をアップロードする
4. ファイルを解析する
5. ベクトルを生成する
6. 検索フレーズを入力する
7. 関連度順で結果を表示する
8. PDF の場合は最関連ページを明示する

## API キーの扱い

初期版は完全フロントエンド型とする。

- ユーザーが API キーを入力する
- 永続保存しない
- `localStorage` にだけ保存する
- キーは送信されず、ユーザーのブラウザの中でだけ安全に扱われることを明記する

理由:

- 最短で動く体験を作れる
- プロトタイプとして十分

## エラー設計

ユーザーに分かりやすく、原因が切り分けられるようにする。

- API キー不正
- API 呼び出し失敗
- PDF ページ数超過
- 非対応ファイル形式
- ファイル解析失敗
- ベクトル生成失敗
- 検索対象が未登録

## 初期版の制約

- PDF はモデル仕様上、1 リクエストあたり 1 ファイル、最大 6 ページ制約を前提とする
- 初期版では小規模デモ向けに最適化する
- 多数ファイルや巨大 PDF の最適化は行わない
- 公開プレビュー機能を使うため、仕様変更リスクを許容する

## 実装判断メモ

### なぜこのスコープか

- 一番分かりやすい価値は「検索フレーズに最も対応した PDF のページの内容が見れる」こと
- そこにテキストファイルも加えることで、RAG 的な関連度比較のデモとして十分成立する
- API キー入力のみで始めることで、提案段階でも試しやすい

### なぜ白基調か

- 企画書代わりに見せる用途では、装飾よりも可読性が重要
- スコアや検索結果の変化が視認しやすい

### なぜレスポンシブ必須か

- 打ち合わせや提案時にノート PC とスマホの両方で見せる可能性がある
- プロトタイプ段階でもレイアウト破綻は避けるべき

## 次にやること

この `AGENTS.md` を前提に、次は以下を進める。

1. React アプリの雛形を作る
2. API キー入力 UI を作る
3. テキスト / PDF アップロード UI を作る
4. `gemini-embedding-2-preview` を使うベクトル化処理を実装する
5. 類似度計算とランキング表示を実装する
6. PDF の最関連ページ表示を実装する
7. 白基調のレスポンシブ UI に整える
