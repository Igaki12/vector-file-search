# AGENTS.md

## 目的

このリポジトリでは、React ベースのウェブアプリとして、企画提案やモック提示に使えるプロトタイプを構築する。

初期版で実現する体験は以下。

- ユーザーが自分の API キーを入力する
- 検索フレーズを入力する
- ベクトル検索と一般的な RAG 的な関連度計算を使って、関連度の高い順に並び替える
- 特に PDF については、どのページが最も関連しているかを直感的に示す
- API 接続確認の段階で、あらかじめ用意したサンプルストレージを自動で読み込み、ベクトル化する
- 必要な場合のみ、追加の PDF / テキストをストレージに加える

この `AGENTS.md` は、以後の実装判断の基準とする。

## 固定方針

- フロントエンドは React
- 初期版は API キー入力のみで開始する
- 初期対応ファイルはテキストと PDF のみ
- Google Drive や外部取り込みは一切考慮しない
- UI は白基調、分かりやすさ優先、レスポンシブ前提
- 企画書代わりに見せられる、シンプルで理解しやすい体験を優先する
- 主導線は「検索」であり、「ファイル追加」はオプション扱いにする
- 主導線は `API キー → 接続確認とサンプル準備 → 検索` とする
- Medteria 外観版は既存検索アプリとは別エントリで管理する
- Medteria 外観版では、当面は PDF プレビュー表現のみを先行実装し、ベクトル検索は持ち込まない

## 参照ドキュメント

2026-03-11 時点で、以下のローカル資料を正式な前提として扱う。

- [text-embed2-official-document.txt](/Users/igaki/Documents/GitHub/vector-file-search/text-embed2-official-document.txt)

この資料は、2026 年 3 月 10 日公開の Google Cloud 公式ドキュメントのコピーであり、`gemini-embedding-2-preview` の仕様を含む。

Medteria 外観版の検討・比較では、以下のローカル参照物も使う。

- [PLAN-introduce-to-Medteria.md](/Users/igaki/Documents/GitHub/vector-file-search/PLAN-introduce-to-Medteria.md)
- [screenshot-medteria.html](/Users/igaki/Documents/GitHub/vector-file-search/screenshot-medteria.html)
- [\_app-473b12f3f682a78f.js](/Users/igaki/Documents/GitHub/vector-file-search/screenshot-medteria_files/_app-473b12f3f682a78f.js)

上記は Medteria の DOM 構造、余白、色調、ボタン寸法、一覧レイアウトを観察するための参照物であり、アプリ本体へそのまま移植するためのものではない。

## モデル前提

初期版では、埋め込みモデルとして以下を採用する。

- モデル ID: `gemini-embedding-2-preview`

上記ドキュメントから採用する重要仕様:

- マルチモーダル入力に対応
- 入力としてテキスト、画像、音声、動画、PDF を受け付ける
- ベクトル出力は最大 3,072 次元
- `output_dimensionality` により出力次元数の調整が可能
- PDF を直接入力できる
- ドキュメント OCR を読む
- ドキュメントは 1 リクエストあたり 1 ファイル、最大 6 ページ
- 最大入力長は 8,192 トークン
- 公開プレビューであり、安定性よりも試作向けの位置づけ

## 初期版のスコープ

### 対応するもの

- テキスト入力の検索
- テキストファイルの埋め込み化
- PDF のページ単位ベクトル化
- 関連度スコアの算出
- スコア順の並び替え
- PDF の最関連ページ表示
- PDF の詳細プレビュー表示
- 既存ストレージの読み込み
- オプションでの追加資料登録
- レスポンシブなデモ UI

### Medteria 外観版で当面対応するもの

- `medteria.html` の別エントリ出力
- Medteria のドライブ一覧風レイアウト再現
- `sample-files/` ベースの静的 PDF 一覧表示
- 右側拡張パネル / 下部シートとしての PDF プレビュー
- 既存トップから Medteria 外観版への遷移導線

### Medteria 外観版で当面対応しないもの

- API キー入力
- ベクトル検索
- 関連度スコア表示
- 検索結果ランキング
- 追加ファイル登録 UI

### 対応しないもの

- Google Drive 連携
- Google Docs / Sheets / Slides 取り込み
- 画像検索 UI
- 音声 / 動画
- ベクトル DB
- ログイン
- サーバー側保存
- 大規模ファイル処理

## 検索の考え方

このアプリは、一般的な RAG に近い検索体験を、分かりやすい UI で見せることを目的にする。

### 採用する検索方式

- ユーザーの検索フレーズをベクトル化する
- 各ファイル、または PDF の各ページをベクトル化する
- ベクトル内積または cosine similarity を使って意味的関連度を計算する
- 必要に応じて、完全一致や部分一致などの lexical score を補助的に使う
- 最終スコアを数値化し、高い順に並べる

### 初期版の推奨スコアリング

初期版では、以下のハイブリッドスコアを基本とする。

- semantic score: ベクトル類似度
- lexical score: 完全一致、部分一致、出現回数などの軽量テキスト一致
- final score: `0.8 * semantic + 0.2 * lexical`

理由:

- 一般的な RAG 検索として説明しやすい
- 意味検索を主体にしつつ、明確な語句一致も拾える
- デモで結果が分かりやすい

## 検索対象ストレージの考え方

### 初期状態

- `sample-files/` にある PDF 群を、デモ用の既定ストレージとして扱う
- API 接続確認に成功した時点で、既定ストレージを自動で読み込み、ベクトル化する
- 同じサンプルは重複登録しない

### 追加登録

- 追加の `.txt` / `.pdf` はオプションとして登録できる
- 短いテキストメモも直接追加できる
- 追加登録 UI は結果一覧より下に配置し、主導線から外す
- 追加された資料は追加時点で解析し、ベクトル化してストレージへ加える

### 保存範囲

- ストレージ内容はブラウザのメモリ上で扱う
- サーバー側や外部ストレージへの永続保存は行わない

## ファイルごとのベクトル化方針

### テキストファイル

対象:

- `.txt`

方針:

- ファイル全体を文字列として読む
- 長文の場合は適切な長さで分割する
- 分割単位ごとにベクトル化する
- ファイル全体の代表スコアは、最上位セグメントのスコアを使う

### PDF

方針:

- PDF はページ単位で分割する
- 各ページを 1 単位としてベクトル化する
- ページごとの関連度を計算する
- PDF 全体の代表スコアは、最も高いページのスコアを採用する
- 検索結果では「どの PDF の何ページが最も関連したか」を明示する

補足:

- 初期版では「PDF は 1 枚ずつ内容を分割してベクトル化する」という要件を最優先する
- 実装上は PDF からページごとのテキストを抽出し、その単位で埋め込みを生成する

## 埋め込み設計

### 次元数

初期版のデフォルトは以下。

- `output_dimensionality = 3072`

必要に応じて将来以下を切り替え可能にする。

- 768
- 1536
- 3072

### 分割戦略

初期版の単位は次のとおり。

- テキスト: セグメント単位
- PDF: ページ単位

長いテキストファイルでは、単一ベクトルではなく複数セグメントに分割する。

推奨:

- 800 から 1,200 文字程度で分割
- 100 から 200 文字程度のオーバーラップを許容

## 類似度計算

### 基本

- ベクトル比較は内積または cosine similarity を使う
- 実装はまず cosine similarity を標準とする
- 正規化済みベクトルを使う場合は内積でもよい

### ファイルの最終順位

テキストファイル:

- 各セグメントのスコアを算出
- 最高スコアのセグメントを代表としてファイル順位を決める

PDF:

- 各ページのスコアを算出
- 最高スコアのページを代表として PDF 順位を決める

表示時には以下を必ず出す。

- ファイル名
- 最終スコア
- 一致したセグメントまたはページ
- 抜粋テキスト

## UI 方針

### デザイン原則

- 白基調
- 余白をしっかり取り、情報を整理する
- 一目で手順が分かる
- 技術者でなくても理解できる
- モバイルでも破綻しない
- 検索中心の導線にする

### 画面構成

#### 1. ヘッダー

- アプリ名
- 簡単な説明
- API キーの保持方針
- 必要最小限の状態サマリー

#### 2. 上段操作エリア

以下を主要操作として見せる。

1. API キー入力
2. 検索フレーズ入力

#### 3. 結果表示

結果表示では以下を優先する。

- 関連度順ランキング
- Top Match の強調表示
- PDF の最関連ページ番号
- PDF 結果カード押下時の詳細プレビュー
- テキスト抜粋
- 類似度スコアの視覚表示
- `RESULTS` セクションだけは濃色の注目ブロックとして見せる

`RESULTS` セクションの見せ方については以下を採用する。

- ページ全体は白基調を維持しつつ、`RESULTS` セクションのみ濃色アクセントを許容する
- セクション見出しは白系の文字で強く見せる
- `result-card` は白カードではなく、少し暗い半透明カードとして表示する
- 検索実行後または結果更新時だけ、約 3 秒の波打つ演出を表示する
- 常時ループアニメーションにはしない
- 動きを減らす設定では、演出アニメーションを抑制できるようにする

PDF の詳細表示については以下を採用する。

- `result-card` のうち PDF 結果のみ押下可能にする
- 押下時は、PDF ビュワーをモーダル風の詳細ペイン / シートとして開く
- 背景全体を暗く覆うオーバーレイは使わない
- 横長画面では画面右側に縦長ペインとして表示し、背景側のアプリ本体は左へ寄せて擬似的なスプリットビューにする
- 縦長画面では画面下側にシートとして表示し、本文と重なりすぎないよう余白で逃がす
- 検索結果から開いた場合は、その PDF の最関連ページを初期表示する
- サンプル PDF 一覧から開いた場合は、先頭ページから表示する
- PDF ビュワーの開閉時には、背景側の移動とビュワーの出入りが分かる軽いアニメーションを入れる
- `.txt` 結果は現時点では押下しても何もしない
- 将来 `.txt` の詳細表示を追加できる余地は残す

レスポンシブ表示については以下も維持する。

- スマートフォン画面でも、ヘッダーのアイコンとタイトルコピーは上下積みではなく 2 列横並びを維持する
- スマートフォン画面でも、状態サマリーは 3 列横並びを維持する
- スマートフォン画面では、主要ボタンは右揃えを基本とする
- ヘッダー左上のアイコンは、タイトルブロック内で上揃えに配置する

#### 4. オプション領域

結果一覧の下に配置する。

- 検索対象サンプルの確認
- 追加ファイルの登録
- 追加テキストの登録
- 主導線を邪魔しない補助的位置づけ

## 体験フロー

1. ユーザーが API キーを入力する
2. 接続確認を行う
3. 接続確認と同時に、既定のサンプル PDF を自動で読み込み、ページ単位でベクトル化する
4. 検索フレーズを入力する
5. 既存ストレージに対して関連度順で結果を表示する
6. PDF の場合は最関連ページを明示し、必要に応じて結果カードから詳細プレビューを開ける
7. 必要な場合のみ、下部から資料を追加する
8. 追加された PDF ページまたはテキストセグメントを、その場でベクトル化する

## API キーの扱い

初期版は完全フロントエンド型とする。

- ユーザーが API キーを入力する
- `localStorage` に保存する
- キーはサーバー側に保存しない
- ブラウザ内だけで扱うことを明記する

理由:

- 最短で動く体験を作れる
- プロトタイプとして十分

## エラー設計

ユーザーに分かりやすく、原因が切り分けられるようにする。

- API キー不正
- API 呼び出し失敗
- 非対応ファイル形式
- ファイル解析失敗
- ベクトル生成失敗
- 検索対象が未登録
- サンプル PDF の取得失敗

## 初期版の制約

- PDF は小規模デモ向けに扱う
- 多数ファイルや巨大 PDF の最適化は行わない
- 公開プレビュー機能を使うため、仕様変更リスクを許容する
- クライアント側処理のため、大きい PDF では時間がかかる場合がある

## GitHub Pages 前提

- ビルド成果物は `docs/` に出力する
- `sample-files/` の内容はビルド後に `docs/sample-files/` にコピーする
- GitHub Pages では `/vector-file-search/` 配下で配信される前提で `base` を設定する
- 既存フロントエンドは `docs/index.html` を維持する
- Medteria 外観版は `docs/medteria.html` に別エントリとして出力する
- 既存フロントエンドから Medteria 外観版へ、画面内ボタンひとつで遷移できる構成を維持する
- Medteria 外観版は既存トップの置き換えではなく、比較・提案用の別導線として扱う

## 実装判断メモ

### なぜ主導線からファイル投入を外したか

- このツールの本質は「すでに並べた資料をフレーズで検索する」ことだから
- 毎回ファイル追加を先に要求すると、検索デモとしての理解が遅くなるから

### なぜ既定ストレージを用意するか

- 打ち合わせや提案の場で、すぐ検索デモを始められるから
- `sample-files/` をそのまま検索対象にできると説明しやすいから
- 接続確認の段階で自動準備すると、検索までの説明が短くなりデモを始めやすいから

### なぜ追加登録をオプションにしたか

- 検索の本筋と補助機能を分離できるから
- UI の見通しが良くなるから

### なぜ Medteria 外観版を別エントリにするか

- 検索デモ本体の体験を壊さずに、見た目提案だけを独立して比較できるから
- 既存 Medteria に似せる調整を、検索ロジックの変更と切り離して進められるから
- 現時点では「見た目再現 + PDF プレビュー」を先に固める方が、提案用途として分かりやすいから

## 更新履歴

### 2026-03-11

- 初版の仕様ドラフトを作成
- `gemini-embedding-2-preview` 前提に更新
- `output_dimensionality = 3072` を初期値として明記
- 対象ファイルを `.txt` と `.pdf` に限定
- PDF をページ単位でベクトル化する方針を明記
- API キーを `localStorage` 保持とする方針に更新
- GitHub Pages 向けに `docs/` 出力を前提へ追加
- `sample-files/` を既定の検索対象ストレージとして扱う方針を追加
- 主導線から「ファイル投入」を外し、検索中心の導線へ変更
- 結果一覧の下に、オプションの追加資料登録領域を置く構成に更新

### 2026-03-12

- 主導線を `API キー → 検索` に明確化
- サンプルストレージ読み込みを主導線から外し、結果一覧より下のオプション領域へ移動
- 画面上部の情報量を減らし、よりシンプルな構成を優先する方針を追記
- API 接続確認成功時に、サンプル PDF を自動で読み込み・ベクトル化する方針へ更新
- 追加ファイルと追加テキストは、登録タイミングで都度ベクトル化する方針を明記

### 2026-03-17

- PDF 結果カード押下時に、最関連ページから開く詳細プレビューを追加
- サンプル PDF 一覧押下時に、別タブ遷移ではなく詳細プレビューを開く方針へ更新
- PDF 詳細表示は、横長画面では右側ペイン、縦長画面では下側シートに出す方針を追記
- `.txt` 結果は現時点では押下非対応とし、将来拡張の余地を残す方針を追記

### 2026-03-19

- PDF 詳細表示は、背景全体を覆うオーバーレイではなく、右側ペインと背景の左寄せで擬似スプリットビューに見せる方針へ更新
- 縦長画面では、下側シートと本文が重なりすぎないよう余白で逃がす方針を追記
- PDF ビュワー開閉時に、背景移動とパネル出入りのアニメーションを入れる方針を追記
- スマートフォン画面でも、ヘッダーのアイコンとタイトルコピーは 2 列横並びを維持する方針を追記
- スマートフォン画面でも、状態サマリーは 3 列横並びを維持する方針を追記
- スマートフォン画面では、主要ボタンを右揃えで見せる方針を追記
- ヘッダーのアプリアイコンを上揃えで配置する方針を追記
- `RESULTS` セクションのみ濃色の注目ブロックとして見せる方針を追記
- `result-card` を暗色の半透明カードとして見せる方針を追記
- 検索実行後または結果更新時だけ、約 3 秒の波打つ演出を表示する方針を追記

### 2026-03-22

- Medteria 外観版の実装計画書 `PLAN-introduce-to-Medteria.md` を正式な参照物として追加
- Medteria 外観参照用のローカル資料は `screenshot-medteria.html` と `screenshot-medteria_files/_app-473b12f3f682a78f.js` を主要参照物として扱う方針を追加
- GitHub Pages では既存トップ `docs/index.html` を維持し、Medteria 外観版を `docs/medteria.html` に別エントリとして出力する方針を追加
- Medteria 外観版では、当面はベクトル検索を載せずに PDF プレビュー表現の再現を優先する方針を追加
